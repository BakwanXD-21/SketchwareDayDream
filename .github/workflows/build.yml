name: Build APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build both flavors
        env:
          GRADLE_OPTS: "-Xmx4096m"
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: List generated APKs
        run: |
          echo "=== Generated APKs ==="
          ls -la app/build/outputs/apk/*/release/*.apk || echo "No APKs found"

      - name: Upload all APKs as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Sketchware-APKs
          path: |
            app/build/outputs/apk/android26/release/*.apk
            app/build/outputs/apk/android33/release/*.apk